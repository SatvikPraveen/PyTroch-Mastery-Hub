# Dependencies are declared once in pyproject.toml. This file exists for tools and
# workflows that expect a requirements file; it installs the project in editable mode.
#
#   pip install -r requirements.txt            # core + dev + notebooks
#   pip install -e ".[all]"                    # every optional extra
#
# For CUDA/CPU-specific torch wheels see https://pytorch.org/get-started/locally/
-e .[dev,notebooks]
